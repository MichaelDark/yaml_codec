name: yaml_codec
description: Encoder and decoder for converting YAML object representation.
version: 1.0.0
repository: https://github.com/MichaelDark/yaml_codec

environment:
  sdk: ^3.5.0

dependencies:
  yaml: ^3.1.3
  yaml_writer: ^2.0.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
